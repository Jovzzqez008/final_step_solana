# ═══════════════════════════════════════════════════════════
# NIXPACKS CONFIGURATION FOR RAILWAY
# ═══════════════════════════════════════════════════════════

[phases.setup]
nixPkgs = ['python312', 'gcc']

[phases.install]
cmds = [
    'pip install --upgrade pip setuptools wheel',
    'pip install -r requirements.txt'
]

[start]
cmd = 'python solana_bot_ml.py'
